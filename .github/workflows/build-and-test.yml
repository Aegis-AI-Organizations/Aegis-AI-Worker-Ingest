name: Module - Build & Test
on: workflow_call

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Install protobuf compiler
        if: hashFiles('Cargo.toml') != ''
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Build
        run: if [ -f Cargo.toml ]; then cargo build --verbose; else echo "No Cargo.toml, skipping"; fi
      - name: Run tests with coverage
        run: |
          if [ -f Cargo.toml ]; then
            cargo install cargo-tarpaulin
            cargo tarpaulin --verbose --fail-under 80 --exclude-files src/main.rs src/workflows.rs
          else
            echo "No Cargo.toml, skipping"
          fi
